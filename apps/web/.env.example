# --- Web environment (Vite) ---
# REST API base URL (includes the /api global prefix)
VITE_API_URL=http://localhost:3000/api

# Socket.io server origin (namespace /prices is appended in code)
VITE_SOCKET_URL=http://localhost:3000
